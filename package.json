{
  "scripts": {
    "frontend:dev": "cd frontend && pnpm dev",
    "frontend:build": "cd frontend && pnpm build",
    "contracts:test": "cd contracts && forge test",
    "contracts:build": "cd contracts && forge build",
    "dev": "pnpm contracts:build && pnpm frontend:dev && pnpm backend:dev",
    "backend:dev": "cd backend && pnpm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/morgan": "^1.9.9"
  }
}
